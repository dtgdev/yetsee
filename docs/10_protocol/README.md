# YetSee Protocol — v1alpha1

The Alpha API advertises `yetsee.ai/v1alpha1` as the compatibility namespace for kernel and plugin contracts. The protocol is intentionally small until real implementation experience justifies expansion.
