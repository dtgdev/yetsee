# Plugin Contract

A YetSee extension declares a manifest, validation behavior, execution contract, explanation and health status.

Alpha unifies existing connector, feature extractor, discovery model and agent registries through `/api/v1/plugins`.

Future plugin categories include semantic rules, ontology packs, graph algorithms, reasoners, recommendation strategies, workflows and visualizations.
