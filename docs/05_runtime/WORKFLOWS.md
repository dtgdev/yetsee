# Workflow Runtime

Alpha ships with one reference workflow:

`intelligence-refresh`

1. recompute reusable features
2. recompute semantic concepts
3. rebuild evidence-backed graph
4. run ensemble discovery

The workflow runtime coordinates; each engine retains its single responsibility.
